#pragma once

template <class T>
class CProxy_IHelloTableEvents : public IConnectionPointImpl<T, &__uuidof(_IHelloTableEvents), CComDynamicUnkArray>
{
	//경고 - 마법사에서 이 클래스를 다시 생성합니다.
public:
};
